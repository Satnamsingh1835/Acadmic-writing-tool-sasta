name: humanize-ai-responses

channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - fastapi=0.116.1
  - uvicorn=0.35.0
  - python-multipart=0.0.20
  - httpx=0.28.1

# CI environment definition
